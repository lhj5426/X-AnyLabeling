# 自由锁定标签快捷键功�?- 实现文档

## 功能概述

添加了一个快捷键功能，可以快速锁定选中的标签，而不依赖于困难标记或预定义的标签列表�?

## 功能特点

### 现有的锁定方�?
1. **预定义标签锁�?*：在设置中配置需要锁定的标签（英文逗号分隔�?
2. **困难标记锁定**：勾�?锁定困难标记"选项，自动锁定所有困难标�?

### 新增的自由锁定方�?
3. **快捷键锁�?*：选中一个或多个标签，按快捷键即可快速锁�?

## 使用方法

### 基本操作

1. **锁定标签**�?
   - 在画布上选中一个或多个标签
   - 按快捷键 `Alt+K`（默认）
   - 选中的标签会被锁定，显示🟡黄色�?
   - 锁定后无法在画布上选中或编�?

2. **解锁标签**�?
   - 方法1：在设置中从"需要锁定的标签"列表中删除该标签
   - 方法2：刷新画布（F5）后，在右侧标签列表中点击解�?
   - 方法3：关闭程序，手动编辑配置文件

### 锁定状态说�?

| 状�?| 红绿灯显�?| 说明 | 可否在画布选中 |
|------|-----------|------|---------------|
| 未锁�?| 无灯 | 从未被锁定过的标�?| �?可以 |
| 锁定 | 🟡 黄色�?| 当前被锁定，无法编辑 | �?不可�?|
| 解锁 | 🔵 蓝色�?| 曾经被锁定，现在解锁�?| �?可以 |

### 锁定效果

锁定后的标签�?
- �?无法在画布上选中
- �?无法移动
- �?无法调整大小
- �?无法删除（普通删除）
- �?可以通过Alt+右键路径删除（强制删除）
- �?根据设置显示/隐藏控制点和控制�?

### 解锁方法

由于锁定的标签无法在画布上选中，所以无法通过快捷键解锁。解锁方法：

1. **通过设置解锁**�?
   - 打开"标签锁定"设置
   - �?需要锁定的标签"列表中删除该标签�?
   - 保存设置

2. **通过右侧标签列表解锁**�?
   - 在右侧标签列表中找到锁定的标�?
   - 右键点击，选择"解锁"（如果有此功能）

3. **刷新画布临时解锁**�?
   - 按F5刷新画布
   - 在刷新后的短暂时间内可以操作
   - 再次刷新会恢复锁定状�?

## 技术实�?

### 修改的文�?
- `views/labeling/label_widget.py`

### 核心代码

#### 1. 锁定选中标签的方�?

```python
def toggle_selected_shapes_lock(self):
    """锁定选中的标�?""
    if not self.canvas.selected_shapes:
        return
    
    # 只锁定未锁定的标�?
    for shape in self.canvas.selected_shapes:
        # 将标签添加到锁定列表
        if shape.label and shape.label.strip():
            Shape.locked_labels.add(shape.label.strip())
            # 清除session解锁标记（如果有的话�?
            shape.is_session_unlocked = False
    
    # 更新配置中的锁定标签列表
    locked_labels_str = ','.join(sorted(Shape.locked_labels))
    self._config.update({"locked_labels": locked_labels_str})
    
    # 刷新显示
    self.label_list.update()
    self.canvas.update()
    self.set_dirty()
```

#### 2. 快捷键动作定�?

```python
toggle_lock = action(
    self.tr("Lock Label"),
    self.toggle_selected_shapes_lock,
    shortcuts.get("toggle_lock", "Alt+K"),
    None,
    self.tr("Lock selected shapes"),
    enabled=False,
)
```

#### 3. 添加到编辑菜�?

```python
editMenu=(
    edit,
    delete,
    copy,
    paste,
    toggle_lock,  # 新增
    cancel_paste_preview,
    ...
)
```

#### 4. 启用/禁用控制

```python
def shape_selection_changed(self, selected_shapes):
    ...
    n_selected = len(selected_shapes)
    self.actions.toggle_lock.setEnabled(n_selected)  # 有选中时启�?
    ...
```

## 快捷键配�?

### 默认快捷�?
- `Alt+K` - 锁定选中的标签（K代表locK�?

### 自定义快捷键

可以在快捷键管理器中修改�?
1. 打开"工具" �?"快捷键管理器"
2. 搜索"toggle_lock"�?Lock Label"
3. 点击修改，设置新的快捷键
4. 保存并应�?

## 使用场景

### 场景1：保护重要标�?
标注了一些重要的对象，不想误操作修改或删除：
1. 选中这些标注
2. �?`Alt+K` 锁定
3. 继续标注其他对象，不用担心误操作

### 场景2：分阶段标注
第一阶段标注完成后，锁定已标注的对象�?
1. 全选第一阶段的标�?
2. �?`Alt+K` 锁定
3. 开始第二阶段标注，不会影响第一阶段的结�?

### 场景3：临时锁�?
标注过程中，某些标签已经确认无误，想临时锁定防止误操作：
1. 选中这些标签
2. �?`Alt+K` 锁定
3. 如果需要修改，通过设置解锁

## 与其他锁定方式的关系

### 优先�?
1. **Session解锁** > **预定义锁�?* > **困难标记锁定**
2. 快捷键解锁会设置 `is_session_unlocked = True`，优先级最�?
3. 快捷键锁定会将标签添加到 `locked_labels` 集合�?

### 组合使用
- 可以同时使用预定义锁定和快捷键锁�?
- 快捷键解锁可以临时解锁预定义锁定的标�?
- 刷新画布（F5）会重置所有session解锁状�?

## 注意事项

1. **锁定是基于标签名�?*：锁定一个标签后，所有同名标签都会被锁定
2. **解锁是临时的**：快捷键解锁只在当前会话有效，刷新画布后会恢复锁�?
3. **配置会保�?*：快捷键锁定会将标签添加到配置文件中，下次打开仍然锁定
4. **强制删除**：即使锁定，也可以通过 Alt+右键路径删除

## 测试建议

1. **基本功能测试**�?
   - 选中标签，按 `Alt+K` 锁定
   - 尝试移动、调整大小、删除（应该无法操作�?
   - 再次�?`Alt+K` 解锁
   - 确认可以正常编辑

2. **多选测�?*�?
   - 选中多个标签，按 `Alt+K` 锁定
   - 确认所有标签都显示黄色�?
   - 再次�?`Alt+K` 解锁
   - 确认所有标签都显示蓝色�?

3. **红绿灯显示测�?*�?
   - 未锁定的标签：无�?
   - 锁定的标签：黄色�?
   - 解锁的标签：蓝色�?

4. **配置持久化测�?*�?
   - 锁定标签后关闭程�?
   - 重新打开，确认标签仍然锁�?

## 实现日期
2026-01-08
